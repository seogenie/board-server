type Query {
  sayBaby(name: String!): Seojin!
  sayHello: String!
}

type Seojin {
  text: String!
  error: Boolean!
  iswhat: Int
}

type User {
  id: Int!
  name: String
  profilePhoto: String
  email: String
  verifiedEmail: Boolean!
  phoneNumber: String
  verifiedPhoneNumber: Boolean!
  password: String
  createAt: String!
  updateAt: String
}
